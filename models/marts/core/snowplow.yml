version: 2
sources:
  - name: snowplow
    database: raw
    schema: snowplow
    freshness:
      error_after:
        count: 1
        period: hour
    loaded_at_field: collector_tstamp
    tables:
      - name: events