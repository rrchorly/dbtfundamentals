version: 2

models:
    - name: stg_customer
      description: Stores customer data
      columns:
        - name: customer_id
          description: Customer customer_id
          tests:
            - not_null